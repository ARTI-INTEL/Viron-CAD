# ── Discord Bot Token ──────────────────────────────────────────
# Get this from https://discord.com/developers/applications
DISCORD_BOT_TOKEN=your_bot_token_here

# ── Discord Application ID ─────────────────────────────────────
# Reuse the same app from your OAuth login
DISCORD_CLIENT_ID=your_discord_client_id

# ── Guild ID (optional, for instant command registration) ──────
# Set this during development so slash commands register instantly.
# Remove for production to register commands globally.
DISCORD_GUILD_ID=your_dev_guild_id

# ── CAD API Base URL ──────────────────────────────────────────
# The public URL of your Viron CAD server
CAD_API_BASE=https://your-cad-domain.com

# ── Shared Secret ──────────────────────────────────────────────
# MUST match DISCORD_BOT_SECRET in the main CAD server's .env
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
DISCORD_BOT_SECRET=generate_a_64_char_hex_string_here

# ── Role-based access control (optional) ───────────────────────
# Set this to a Discord role ID to restrict /lookup-plate,
# /lookup-person, and /infractions to only users with that role.
# Leave empty to allow any server member to use these commands.
# LEO_DISCORD_ROLE_ID=
LEO_DISCORD_ROLE_ID=
